# Post Cockpit

Browser app to build a weekly Substack issue + Bluesky post pack.
No login. Autosaves locally in your browser (localStorage).

## Run locally
```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt
python app.py
